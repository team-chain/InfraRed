# ===== Backend (Ingestion API + Workers) =====
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
# python-multipart: 0.0.20+ fixes Arbitrary File Write, DoS via boundary, unbounded headers (Dependabot #20/#8/#34, High)
python-multipart==0.0.20

# Auth
# python-jose: 3.5.0+ fixes CVE algorithm confusion on OpenSSH ECDSA keys (Dependabot #4, Critical)
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
# cryptography: 44.0.1+ fixes SECT curves subgroup attack (Dependabot #21, High)
cryptography==44.0.1

# MFA / TOTP
pyotp==2.9.0
qrcode[pil]==8.0

# Redis
redis==5.2.0

# PostgreSQL
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36

# HTTP client
httpx==0.27.2
# aiohttp: 3.12.x fixes zip bomb auto_decompress + multiple multipart/header DoS (Dependabot #11/#27 et al., High)
aiohttp==3.12.15

# AWS Bedrock / S3
boto3==1.35.71

# Logging / metrics
structlog==24.4.0
prometheus-client==0.21.0

# GeoIP
geoip2==4.8.0

# PDF report (WeasyPrint)
# weasyprint: 65.x fixes SSRF Protection Bypass via HTTP Redirect (Dependabot #19, High)
weasyprint==65.1

# Stripe billing
stripe==11.1.1

# SSO (WorkOS) — optional, graceful fallback if not installed
# workos==4.4.0

# UEBA (Numpy-based autoencoder — no ML framework dependency)
numpy==2.1.3

# SIGMA rule parsing
PyYAML==6.0.2
sigma-cli==1.0.0; python_version>="3.9"

# Integrations (Slack, PagerDuty, Jira, Splunk)
slack-sdk==3.33.3

# Util
python-dotenv==1.0.1
# orjson: 3.10.18 fixes deeply-nested JSON recursion limit (Dependabot #5, High)
orjson==3.10.18

# ──────────────────────────────────────────────
# Test dependencies
# ──────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
fakeredis==2.26.1
ruff==0.8.3
