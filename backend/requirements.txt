# ===== Backend (Ingestion API + Workers) =====
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
# python-multipart: 0.0.29+ fixes Arbitrary File Write (#20), DoS unbounded headers (#34), DoS preamble (#33)
python-multipart==0.0.29

# Auth
# python-jose: 3.5.0+ fixes CVE algorithm confusion on OpenSSH ECDSA keys (Dependabot #4, Critical)
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
# email-validator: pydantic EmailStr 의존 (verification_routes.py)
email-validator==2.2.0
# cryptography: 48.0.0 — fixes SECT curves subgroup attack (#21, High) + later CVEs
cryptography==48.0.0

# MFA / TOTP
pyotp==2.9.0
qrcode[pil]==8.0

# Redis
redis==5.2.0

# PostgreSQL
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36

# HTTP client
httpx==0.27.2
# aiohttp: 3.13.5 — fixes zip bomb auto_decompress (#11, High) + ALL moderate/low aiohttp CVEs
# (#15, #16, #17, #23, #25, #26, #27, #28, #29, #30, #31, #32 + others)
aiohttp==3.13.5

# AWS Bedrock / S3
boto3==1.35.71

# Logging / metrics
structlog==24.4.0
prometheus-client==0.21.0

# Error tracking (Sentry) — graceful no-op if SENTRY_DSN not set
sentry-sdk[fastapi]==2.19.2

# GeoIP
geoip2==4.8.0

# PDF report (WeasyPrint)
# weasyprint: 68.1 — fixes SSRF Protection Bypass via HTTP Redirect (#19, High)
weasyprint==68.1

# Stripe billing
stripe==11.1.1

# SSO (WorkOS) — optional, graceful fallback if not installed
# workos==4.4.0

# UEBA (Numpy-based autoencoder — no ML framework dependency)
numpy==2.1.3

# SIGMA rule parsing
PyYAML==6.0.2
sigma-cli==1.0.0; python_version>="3.9"

# Integrations (Slack, PagerDuty, Jira, Splunk)
slack-sdk==3.33.3

# Util
# python-dotenv: 1.2.2 — fixes symlink-following set_key arbitrary file overwrite (#7, Moderate)
python-dotenv==1.2.2
# orjson: 3.11.9 — fixes deeply-nested JSON recursion DoS (#5, High)
orjson==3.11.9

# ──────────────────────────────────────────────
# Test dependencies
# ──────────────────────────────────────────────
# pytest: 8.4.2 — fixes tmpdir handling vuln (#6, Moderate); stays on 8.x major to avoid breakage
pytest==8.4.2
pytest-asyncio==0.24.0
fakeredis==2.26.1
ruff==0.8.3
